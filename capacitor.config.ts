import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'corte_de_notas_briyith',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
